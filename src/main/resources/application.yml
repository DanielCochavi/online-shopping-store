spring:
  application:
    name: online-shopping-store

debug: false

logging:
  level:
    root: INFO
    org.springframework: INFO

security:
  jwt:
    secret: ${JWT_SECRET:dev-only-change-me-in-production-dev-only-change-me-in-production}
    expiration: PT1H

springdoc:
  swagger-ui:
    path: /swagger-ui.html
